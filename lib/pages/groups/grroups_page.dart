import 'package:flutter/material.dart';

class GrroupsPage extends StatelessWidget {
  const GrroupsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Groups")));
  }
}
